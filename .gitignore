# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist-electron
release
*.local

# Env & secrets (do not commit)
.env
.env.*
!.env.example

# electron-builder may regenerate these from build/icon.png
build/*.ico
build/*.icns

# Tooling / OS noise
# Vite / vite-plugin-electron 加载 ts 配置时的临时产物（含本机路径）
vite.config.ts.timestamp-*.mjs
*.tsbuildinfo
Thumbs.db
Desktop.ini
coverage
.eslintcache

# Editor directories and files
.vscode/.debug.env
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 若误用 npm/yarn 生成锁文件可忽略；pnpm-lock.yaml 需提交，供 CI 缓存与 --frozen-lockfile
package-lock.json
yarn.lock
/test-results/
/playwright-report/
/playwright/.cache/
